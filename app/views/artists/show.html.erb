<h1><%= @artist.name %></h1>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, song_path(song) %></li>
    <li><%= link_to "#{song.title} through Artist", artist_song_path(@artist, song) %></li>

  <% end %>
</ul>
